local ReplicatedStorage = game:GetService("ReplicatedStorage")
local createApps = require(ReplicatedStorage.shared.modules.react.apps)
local receiveReplication = require(ReplicatedStorage.shared.modules.matter.receiveReplication)
local start = require(ReplicatedStorage.shared.modules.matter.start)
local world, state = start(ReplicatedStorage.shared.modules.matter.systems)
receiveReplication(world, state)
createApps()